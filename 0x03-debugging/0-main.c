#include "main.h"

/**
 * main - test positive or negative print function
 * Description: Debugging tests for void positive_or_negative(int i) function
 * Return: 0 (Success)
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
